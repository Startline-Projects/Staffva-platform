/**
 * The two clocks the signed agreement defines. One definition — the notice
 * route, the pause route, the completion cron, and every UI chip must never
 * disagree about how long these run.
 */
export const NOTICE_DAYS = 14;
export const PAUSE_AUTO_END_DAYS = 30;
