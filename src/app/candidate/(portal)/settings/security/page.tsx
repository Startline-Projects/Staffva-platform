import SecuritySettings from "@/components/account/SecuritySettings";

/** Inside the (portal) group, so it wears the Atlas shell like every other
 *  candidate page — and sits beside settings/notifications, where a candidate
 *  would look for it. */
export default function CandidateSecurityPage() {
  return <SecuritySettings />;
}
